const assert = require('assert');
const checkpackage = require('../index.js');

describe('checkpackage', () => {
  it('has a test', () => {
    assert(false, 'checkpackage should have a test');
  });
});
